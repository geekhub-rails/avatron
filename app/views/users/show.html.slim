br
h7= t(:profile_page)
br
br

ul#user_phones
  = render partial: 'phone_numbers/phone_number', collection: @user.phones

#user_phone_form
	button data-url=new_phone_number_path data-remote=true id='add_number_btn' ➕ Add another phone number

div#user_email_style
	= render 'edit'

= button_to t(:remove_acc), '/profile', method: "delete", remote: true, data: {confirm: 'Are you sure?'}, id: 'remove_account_btn'
